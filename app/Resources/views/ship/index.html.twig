{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ship list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Available</th>
                <th>Color</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ship in ships %}
            <tr>
                <td><a href="{{ path('ship_show', { 'id': ship.id }) }}">{{ ship.id }}</a></td>
                <td>{{ ship.name }}</td>
                <td>{% if ship.available %}Yes{% else %}No{% endif %}</td>
                <td>{{ ship.color }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('ship_show', { 'id': ship.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('ship_edit', { 'id': ship.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ship_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
